# 对照：洞察落成定律

对应：`LAND-1` `LAND-2` `LAND-3` `LAND-5` `LOOP-6`

**表面症状：** 排障结束。聊天里已经有一句「这是优势」或「分类要先写」。窗口要关了。

**完成定义：**

1. 洞察有稳定门禁 id，引用用 id 不用段落标题（`LAND-1`）。
2. 同一变更写入 `dimensions/` 或 `examples/`，不只留在聊天（`LAND-2`）。
3. `CHANGELOG.md` 有对应 `## version`；`catalog.json`、`gates.json`、`package.json` 的 version 一致（`LAND-3`）。
4. `npm run verify` 绿（`LAND-5`）。
5. 本会话只提取定律；七步执行归 build-standard / `PROBE-*`；收尾有新 id 或新例子（`LOOP-6`）。

本文件本身就是 `LOOP-6` 要求的那种例子：0.2.0 把重叠门禁废止之后，用对照把现行 id 钉住。

**不要写进本文件的：** 密钥、真实账号、内网地址。
