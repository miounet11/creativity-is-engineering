# 纬度：`<id>`

> 复制本文件为 `dimensions/<id>.md`。**先在 `gates.json` 里加门禁 id**，再写这里，然后 `npm run verify`。

## 一句话

**用现在时写：做对时必须出现什么可观察结果。**

## 何时用

- 什么阶段打开这份纬度
- 和哪些已有纬度一起用（不要重复它们的门禁）
- 上线判定去 [ship-standard](https://github.com/miounet11/ship-standard)，不要把本仓写成第二份 launch

## 验收句（必须能写成检查）

1. …
2. …

## 门禁

- [ ] `XXX-1` …
- [ ] `XXX-2` …

## 禁止

- 不要…
- 不要…

## 产出

过关时必须留下的证据：门禁 id、例子对照、生成的 STATUS，而不是「聊完了」。
