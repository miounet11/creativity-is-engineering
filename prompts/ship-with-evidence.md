# 给 AI：带着证据上船

「能跑」不是上线。上线前你必须能指出：

- 安装包路径和版本号（与上一包可区分）
- 配置的三种加载方式：URL / 粘贴 / IP
- STOP 路径
- 回滚条件（写死的句子，不是「看情况」）
- 至少一条主路径成功样本，或明确说还在灰度

远程网络失败时，先试已有镜像（IP / HTTP），再写新下载器。  
打包后的 ESM worker 找不到包时，先查 asar 边界，再怀疑没装依赖。

公告只发：安装地址、配置地址、怎么停。不发密钥。
